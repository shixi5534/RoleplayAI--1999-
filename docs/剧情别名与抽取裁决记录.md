# 剧情 GraphRAG · 别名与抽取裁决记录

> 本文件记录**人工裁决结论**，防止同一问题被反复推翻。
> 上游：`docs/剧情术语对照词表.md`、`data/lore/wu_ming_zhe/plot_glossary.json`
> 决策人：用户 ｜ 记录日期：2026-09-11

---

## 裁决 1：凯拉 / 塞西莉 / 莉莉 —— **保留为「无名者」的直接别名**

**结论**：维持现状，不改挂「化名」关系，不从 `plot_aliases.json` 剔除。

**当前数据实况**（`data/lore/wu_ming_zhe/plot_aliases.json`）：

```json
"无名者": ["Ms. Stranger", "Miss Stranger", "Ms. Grace", "淑女格蕾丝",
           "陌生人小姐", "无名之人", "格蕾丝", "莉莉", "凯拉", "塞西莉"]
```

`plot_glossary.json` 的 `terms.无名者` 与之同步，`status: verified`，
来源 `https://res1999.huijiwiki.com/wiki/无名者`。

**为什么这个决定有争议**（记录理由，便于日后复核）：
N07 存在「遭遇真实的凯拉」这类语境，剧中存在**冒充叙事**——
「凯拉」「塞西莉」「莉莉」可能是无名者使用过的身份，也可能是独立人物。
直接并成别名有串味（把别人的事迹算到无名者头上）的风险。

**选择保留的判断依据**：别名归并只取并集、可回滚；而改挂「化名」关系需要
先确认叙事事实，属于 M1a 联网核查的范畴，不该在数据层先行决定。

**复核触发条件**（满足任一即重新审议）：
1. M1a 联网核查时，灰机 wiki 出现独立的「凯拉」「塞西莉」「莉莉」角色条目；
2. N1 云端抽取完成后，这三个名字在语料中出现了与无名者**矛盾的上下文**
   （如同时同地出现、被他人以第三人称分别指称）；
3. Golden Query 中出现「凯拉是谁」类查询且召回质量明显异常。

---

## ✅ 已解决：格蕾丝 / Ms. Grace —— **全部并入无名者别名链**

**结论**：`淑女格蕾丝` 不再独立成节点，其全部表层形式并入「无名者」。

**wiki 查证（灰机 wiki 为唯一标准）**：

- `https://res1999.huijiwiki.com/wiki/淑女格蕾丝` → **该页面不存在**（「本页面目前没有内容」）。
  原术语表给它标 `status: verified` 依据的是 `其他角色一览/2.x`，并非专属页。
- `其他角色一览/2.x`「2.0 飞驰！明日之城」章节原文：

  ```
  ### 淑女格蕾丝
  Ms. Grace
  无名者
  3.7版本加入征集  ID:6249
  ```

  第三行即真实身份。同页相邻条目「金伯利女郎 / Ms. Kimberly / **天使娜娜**」是同一格式，
  可确认该栏位语义就是「本名／真实身份」。**wiki 明确证实淑女格蕾丝 = 无名者。**
- 无名者专属页（ID 3147）通篇未提「格蕾丝」或「Grace」，且写明「没有名字，过往分崩离析的特工」。
  页面里出现的「淑女的假面」是**传承技能名，不是人名**，不可作为别名。

**已执行**：

| 文件 | 改动 |
|---|---|
| `plot_aliases.json` | 无名者别名 += `Ms.Grace` / `Madam Grace` / `Grace` / `Miss Grace` |
| `plot_glossary.json` | `alias_flat` 删除独立的 `淑女格蕾丝` 组（55 组 → 54 组），全部并入 `无名者`；`terms.淑女格蕾丝` 保留仅供审计，note 标注「已并入，勿据此重建独立组」 |
| 图谱 | `--rebuild-graph-only` 零 LLM 重建，僵尸节点 `Grace` / `Miss Grace` 已清除 |

**重建后实测**：实体 5026→5024，边 4035→3746。
`grace` / `miss grace` / `ms. grace` / `ms.grace` / `madam grace` / `淑女格蕾丝` / `格蕾丝`
**全部指向无名者**，无名者 mentions 18→20（吸收两个僵尸节点）。

---

## 裁决 2：云端抽取走「对话分批产出」，不改 `.env`

**结论**：`scripts/cloud_extract.py --dump` → 云端强模型（在对话中产出）→ `--apply`。
不接第三方 API，不修改 `.env`。

**背景**：`.env` 当前是本地 ollama `qwen2.5:7b`，实测
`entity_precision 0.675` / `relation_precision 0.111` / `recall 0.15`——
约 89% 的关系是错的。

**为什么这个方案可行且安全**：
- 抽取缓存的键是 **chunk 文本 sha1**，与模型、来源无关 → 任何来源的产出
  只要按格式写进 `plot_cache/<cid>/<hash>.json`，`--build-graph` 就能零 LLM 重放；
- 全程不启服务、不装依赖、不花钱、可随时暂停；
- 每批 `--dry-run` 先验，确认无误再落盘。

**流程**：

```bash
python -X utf8 scripts/cloud_extract.py --plan --size 40          # 看总量与批次数
python -X utf8 scripts/cloud_extract.py --dump --batch 0 --size 40 # 导出 md
#   → 由云端强模型产出 _cloud/<cid>/batch_000.jsonl
python -X utf8 scripts/cloud_extract.py --apply --batch 0 --dry-run # 先验
python -X utf8 scripts/cloud_extract.py --apply --batch 0           # 入库
python -X utf8 scripts/cloud_extract.py --status                    # 查进度
```

**硬约束已内置到 dump 的规范段**（违反即被 `--apply` 静默丢弃）：
7 类 `type` 白名单、77 条受控谓语、端点必须在本块实体内、禁自环
（**含正名与别名之间、别名与别名之间**）、禁代词泛指。

---

## ⚠️ 重建引入的副作用：正典覆盖率 11/19 → 10/19

**现象**：`tests/test_wiki_canonical_coverage.py` 基线从 plot 11、并集 17 下调为
**plot 10、并集 16**，并把 `无名者 —隶属— 圣洛夫基金会` 登记进 `KNOWN_GAPS`。

**根因（与格蕾丝合并无关）**：重建前 plot 图里有 **269 条 `relation=="待审核"` 的边**，
是早期版本遗留。`canonicalize_relation("待审核")` 恒返回 `None`（该词不在 77 条受控词表内，
只是 `REVIEW_SENTINEL` 哨兵），所以**任何一次干净重建都会把它们全部清除**——
总结 B7 早已预判（「边数 4 035 降至约 3 766，这是预期而非回归」）。
这条被扣的「隶属」原先只以「圣洛夫基金会 —待审核→ 无名者」的形态存在，本就不该计正典命中。

**恢复路径**：N1 云端抽取产出明确的「隶属」边后，把基线调回 11 / 17 并从 KNOWN_GAPS 移除。
已在测试文件里写明，不会无声遗忘。

## 未决事项

- **非主线语料口径**：plot 层 65 chunks vs lore 层 61 条，两者的处理策略与
  验收阈值需分别设定。M2 落地 `source_kind` 后再定。
- **凯拉「身份替代」事实已无法表达**：裁决 1 把凯拉并入无名者后，
  `无名者 —身份替代—> 凯拉` 变成自环、建图时被丢弃，wiki 正典审计对该条返回
  `SAME_ENTITY`。这是「别名归并」与「身份替代叙事」的固有冲突：
  并成同一节点就无法表达「A 冒充/替代 B」。若日后需要用边表达该层叙事，
  需把凯拉改回独立节点 + 身份替代边（即推翻裁决 1）。目前按裁决 1 维持。
