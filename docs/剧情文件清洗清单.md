# 后续剧情文件清洗清单

> **生成时间**：2026-09-10
> **配套文档**：`剧情术语对照词表.md`（术语基准）、`剧情清洗注意事项.md`（约束）、`剧情时间轴与人物去向图.md`、`剧情时间轴与GraphRAG联动.md`
> **项目路径**：`C:\Users\Lenovo\WorkBuddy\2026-07-27-16-45-03\roleplay-ai`
> **图例**：🔴 严重 ／ 🟡 中等 ／ 🟢 建议　　**待补充**标记＝语料仍在采集或事实待核实，暂不处理

## 一、问题类型编码

| 编码 | 问题类型 | 说明 |
|---|---|---|
| **T1** | 节点分裂（ASR 音变/拼写变体） | 同一实体在 `norm_name` 下仍是不同键，分裂成多节点 |
| **T2** | 中英跨语言未归并 | 同一实体中文名与英文名并存为两个节点 |
| **T3** | 类型标签失控 | 42 种类型，含 `角色\|物品\|地点\|组织\|概念\|事件` 等脏值 |
| **T4** | 关系词未归一 / 语义重叠 | 前往/隶属/隶属于/属于/是 等 |
| **T5** | 泛化实体与噪声 | ASR 同音噪声、泛指名词、代词短语 |
| **T6** | 来源混杂 | 主线 ASR 与 UP 主考据解读视频未区分 |
| **T7** | 元数据缺失 | `version` / `arc` / `source_kind` 缺失 |
| **T8** | 语料缺采 | 某版本 chunk 数显著偏低，采集中 |
| **T9** | 关系方向错误 / 否定关系 | 抽取把语义抽反或抽出否定谓语 |
| **T10** | 别名污染 | 乱码、提示词模板泄漏、整句当别名 |
| **T11** | 时间维度缺失 | 图谱节点/边无剧情内时间字段 |
| **T12** | 冗余备份占位 | 历史备份文件占用大量磁盘 |

---

## 二、清洗清单（表一）

| # | 待清洗文件 / 对象 | 规模 | 问题类型 | 严重度 | 处理方式 | 挂接点 | 前置依赖 | 状态 |
|---|---|---:|---|---|---|---|---|---|
| **C1** | `data/knowledge/plot_cache/wu_ming_zhe/*.json` | 2 477 文件 / 10 107 实体提及 | **T1** 节点分裂 | 🔴 | 按 `plot_glossary.json → alias_flat` 归并 16 组变体簇；重点 E1（重塑之手 16 变体/82 次）、E13（暴雨 4 变体/137 次）、E14（基金会 4 变体/173 次） | `plot_graph.py` 归并键 `norm_name`；`build_plot_graph.py L161 load_alias_table` | 术语表 alias_flat | ⬜ 待执行 |
| **C2** | 同上 | 2 937 提及（29.1%） | **T2** 中英跨语言未归并 | 🔴 | 中文规范名与英文官方名双向挂接，归入同一 `entity_id` | `GraphStore.bind_alias_table` (graph_store.py L265) | 术语表 | ⬜ 待执行 |
| **C3** | 同上（type 字段） | 42 种类型 | **T3** 类型标签失控 | 🟡 | 强制收敛到 7 类白名单 `角色/组织/地点/概念/物品/事件/时间`；脏值（`角色\|物品\|地点\|组织\|概念\|事件`、`技能`、`颜色`、`系列` 等 35 种）→ 按上下文重判，无法判定则回落 `概念` | `_JSON_SPEC`（plot_graph.py L56）改为硬白名单校验 | 术语表 `type_whitelist` | ⬜ 待执行 |
| **C4** | `plot_graph_wu_ming_zhe.json`（边） | 4 302 边 | **T4** 关系词未归一 | 🟡 | 已有 `CANONICAL_RELATIONS`(77) 与 `canonicalize_relation`；补充「隶属／隶属于／属于」合并为「隶属于」、「是／为」改为类型断言而非边 | `relation_vocab.py L46 / L477` | — | ⬜ 待执行 |
| **C5** | `plot_cache` + 图谱 | `noise.confirmed` 30 项 + `generic_zh` 14 项 + `pronoun_en` 9 项 | **T5** 泛化与噪声 | 🟡 | 命中 `plot_glossary.json → noise` 的直接删除；`pending_review` 项（Adele/Celia/Felicienne 等 39 项）**保留待判定**，禁止一刀切删除 | `plot_graph.is_noise_entity` (L143) | F1 NPC 译名确认 | ⬜ 待执行（部分 **待补充**） |
| **C6** | 9 个非主线文档（见 §三） | 65 chunks / 5 个 UP 主 | **T6** 来源混杂 | 🔴 | 加 `source_kind: analysis`；主线 ASR 标 `canon`。检索默认只取 `canon`，`analysis` 仅作补充证据并降权 | `plot_corpus.py` `_parse_transcript_file` (L415) + `PlotChunk` | — | ⬜ 待执行 |
| **C7** | `plot_corpus_wu_ming_zhe.json` 中 3 个无版本篇章 | 聚合浪潮 124 / 翡冷翠之春 71 / 入雅典记 62 chunks | **T7** 元数据缺失 | 🟡 | 补 `version` 与 `arc`；确认属支线/活动/间章 | `plot_corpus.parse_title_meta` (L87) | **待补充**：需确认篇章归属 | ⬜ **待补充** |
| **C8** | 3.4「不老春」语料 | 73 chunks / 22 页（同量级版本 150–250） | **T8** 语料缺采 | 🟡 | 补采该版本完整剧情视频 | `PlotCorpus.build` (L238) 三重白名单 | **待补充**：仍在采集中 | ⬜ **待补充** |
| **C9** | 3.6「人们向何处去」语料 | 112 chunks / 14 页 | **T8** 疑似缺采 | 🟢 | 复核是否完整，比对官方章节长度 | 同上 | **待补充** | ⬜ **待补充** |
| **C10** | `data/knowledge/_audit/failed_chunks_22.json` | 22 块 | **T8** 抽取失败 | 🟡 | 沿用 `apply_failed_chunks_18.py` 模式：云端补抽 → 写回缓存 → 零 LLM 重放 | `GraphCache` (graph_extract.py L183) 幂等 | — | ⬜ 待执行 |
| **C11** | `data/knowledge/_audit/empty_batch_*.txt` | A 196 / B 166 / C 46 | **T8** 空块 | 🟢 | 上一轮已补抽 395 块、确认 NOOP 13 块；本轮复核是否仍有残留 | `noop_confirmed.json` | — | ✅ 已完成（待复核） |
| **C12** | 图谱边（历史遗留） | 见核对报告 P1/P2/P8 | **T9** 方向错误 | 🟡 | 已修 3 条（`Igor—是→my father`、`Miss Stranger—持有→Foundation`、`Manus Vindictae—持有→Arcana`→效忠）；补充「否定谓语」审计规则防再生 | `audit_full_corpus.py` | — | ✅ 已修复（规则待补） |
| **C13** | 实体别名（历史遗留） | 10 个污染实体 | **T10** 别名污染 | 🟢 | 已删（`Lucy (Miss Stranger)`、`斯特्र昂德`、提示词泄漏、中文整句别名） | `fix_huiji_conflicts.py` | — | ✅ 已完成 |
| **C14** | `plot_graph_wu_ming_zhe.json` 节点与边 | 5 095 节点 / 4 302 边 | **T11** 时间维度缺失 | 🟡 | 在实体/边上补 `story_time`（剧情内时间）与 `narrative_order`（叙事序）；`edges_of` 当前按插入 ts 排序，非剧情时间 | `build_plot_graph` 调 `upsert_edge` (L649) | 时间轴文档 | ⬜ 待执行 |
| **C15** | 备份文件群 | `plot_cache_bak_prehalluc`(2 477) / `plot_cache_bak_prehuiji`(2 477) / `plot_graph_*.bak_*`(4 个) / `lore_*.bak_voicelore` | **T12** 冗余备份 | 🟢 | 确认无回滚需求后归档到 `data/knowledge/_archive/`，勿直接删除 | — | 需人工确认 | ⬜ 待执行 |
| **C16** | `lore_wu_ming_zhe.json` 58 MB / `graph_wu_ming_zhe.json` 41 MB | — | **T12** 体积 | 🟢 | 检查是否含冗余 vec（768 维 × 大量节点），考虑外置向量或降维 | `GraphStore.save` (L133) | 性能评估 | ⬜ 待评估 |
| **C17** | `data/lore/wu_ming_zhe/剧情脉络与关键事件.md` | 2 处 | **T7** 译名不一致 | 🟡 | 「兀尔德」与 ASR「乌尔德」用字冲突，确认后统一（详见术语表 F3） | — | **待补充**：wiki 无独立条目 | ⬜ **待补充** |
| **C18** | `data/lore/wu_ming_zhe/plot_aliases.json` | 60 条 | **T7** 覆盖不足 | 🔴 | 与 `plot_glossary.json → alias_flat`（51 组 / 约 150 个表层形式）合并，**按 key 取并集，不得覆盖** | `load_alias_table` | 先备份 | ⬜ 待执行 |
| **C19** | `data/lore/wu_ming_zhe/wiki_zh_en.json` | 140 条 | **T7** 仅含可玩角色 | 🟡 | NPC 与组织/地点/概念缺失，需扩充或标注适用范围 | `triage_empty_chunks.py` | F1 | ⬜ 待执行 |
| **C20** | `data/lore/wu_ming_zhe/视频文案/*.md` | 若干 | **T6** 来源混杂 | 🟢 | 考据类文稿仅作译名反查依据，**不得**作为事实抽取语料入图 | — | — | 🟢 规则已明确 |

---

## 三、C6 详单：非主线来源文档（9 个 / 65 chunks）

| doc_id | UP 主 | 发布日期 | chunks | 内容性质 | 建议 `source_kind` |
|---|---|---|---:|---|---|
| `BV12u5M6uEm3_p1` | 重返未来1999（官方） | 2026-05-15 | 1 | 无名者 PV（英） | `canon`（官方 PV） |
| `BV12u5M6uEm3_p2` | 重返未来1999（官方） | 2026-05-15 | 1 | 无名者 PV（中） | `canon`（官方 PV） |
| `BV196ReBxEq9` | 薯条小叔叔 | 2026-05-05 | 24 | 万字全主线解析 | `analysis` |
| `BV1CaM36bEax` | 众口难调有口难开 | 2026-07-08 | 4 | 暴雨时间线梳理（考据） | `analysis` |
| `BV1iT421D7S8` | 银发三千雪满头 | 2024-05-17 | 7 | 剧情入坑讲解 | `analysis` |
| `BV1m6421M7bU` | 荟小荟 | 2024-02-24 | 20 | 保姆级解说 | `analysis` |
| `BV1aX4y117Ph` | 介慈啊 | 2023-05-16 | 3 | 血统解析（推测向） | `analysis` |
| `BV1nyUxBFEpU` | 梦驿九际-凌痕 | 2025-11-22 | 1 | 阵营及纲领介绍 | `analysis` |
| `BV1ZT411V72Q` | 账号已注销 | 2022-06-06 | 4 | 世界观名词科普 | `analysis` |

> **风险**：`analysis` 类视频含 UP 主主观推断，其表述会被抽取器当作剧情事实写入图谱，
> 污染「暴雨是什么」「谁免疫」一类核心问答。**必须在语料层打标，而非在图谱层事后清理。**

---

## 四、执行顺序建议

```
第 0 步  备份        plot_aliases.json / plot_cache/ / plot_graph_*.json
第 1 步  C18        合并术语表（alias_flat → plot_aliases.json）
第 2 步  C1/C2      零 LLM 重放重建，观察节点分裂是否收敛
第 3 步  C3/C4      类型白名单 + 关系归一（需改代码，先跑测试）
第 4 步  C6/C7      语料层补 source_kind 与 version（需重抽或仅改元数据）
第 5 步  C5         噪声清理（pending_review 项待 F1 确认后再动）
第 6 步  C14        挂时间轴（见时间轴文档）
第 7 步  C10/C11    补抽收尾
第 8 步  C15/C16    归档与体积优化
```

**每步之后必跑**：
```bash
.venv/Scripts/python.exe -X utf8 scripts/audit_full_corpus.py
.venv/Scripts/python.exe -X utf8 scripts/acceptance_plot_graph.py
pytest -k "plot or graph"
```

---

## 五、待补充项汇总（语料仍在采集 / 事实待核实）

| 编号 | 待补充内容 | 阻塞的清洗项 | 说明 |
|---|---|---|---|
| **S1** | 主线 NPC 官方译名（Celia / Felicienne / Muriel / Krutov / Maxim / Vanya / Adler / Senato / Lucian / Erd / Andreas / Teresita / Marguerite / Pericles / Typhon 等 15+ 名，合计 190+ 次提及） | C1、C5 | 灰机 wiki「角色列表」仅收录可玩角色，NPC 无独立条目。需从视频文案或游戏文本反查 |
| **S2** | 聚合浪潮 / 翡冷翠之春 / 入雅典记 的版本号与篇章归属 | C7 | 共 257 chunks 无法在时间轴上定位 |
| **S3** | 3.4「不老春」完整语料 | C8 | 仅 73 chunks，显著偏低，**采集中** |
| **S4** | 3.6「人们向何处去」完整性复核 | C9 | 112 chunks，**待复核** |
| **S5** | 兀尔德 / 乌尔德 官方用字 | C17 | wiki 无独立条目 |
| **S6** | 3.8「世纪末尺度」/ 3.9「重燃！流金之海」的剧情内年代 | C14、时间轴 | 无法在时间轴上定位 |
| **S7** | 梅蕾尔·范德维尔 / 夜巡特遣管理局 / 人类党团 的官方英文名 | C1 | 中文已核实，英文为推测 |
| **S8** | 2.6 疯癫与文明 及其余各版本的剧情内年代 | C14、时间轴 | 仅 2.7（1987）与部分事件可确认 |
| **S9** | 无名者幼年组织、代号「普帕」「飞蛾」的官方说明 | 术语表 A5 | wiki 无名者页未提及，来源为二手考据 |
