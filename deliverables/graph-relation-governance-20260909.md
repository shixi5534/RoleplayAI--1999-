# 图谱关系命名系统性治理报告（三步执行 + 量化验证）

日期：2026-09-09 ｜ 角色：wu_ming_zhe ｜ 抽取模型：qwen2.5:7b（本地 Ollama）

---

## 一、每步改动与脚本位置

### 第一步：离线归一化 plot 图谱（不改变抽取逻辑）
- 新增规则源 **`src/roleplay/core/knowledge/relation_vocab.py`**：
  - **77 个 canonical 谓语**（按 10 组分类：身份属类 / 空间移动 / 器物持用 / 人际情感 / 冲突行动 / 认知言语 / 指挥规则 / 事件因果 / 影响状态 / 评价态度）
  - **1038 条同义词映射**（隶属←属于/隶属于/所属/成员…；前往←去往/抵达/来到/赶往…）
  - **卫生过滤**：否定短语开头（不/非/无/未/没/勿/莫…）→ 丢弃；>8 字 → 丢弃；含标点（含省略号占位）→ 丢弃；含拉丁/数字 → 丢弃；垃圾黑名单（不、不与、不会、不同于、不属于、不隶属于、行心力、用 RFID 等）→ 丢弃
  - 判定顺序「先否后是」：否定判定先于子串规则，防止「不隶属于」被救成「隶属」
- 新增一次性脚本 **`scripts/normalize_graph_relations.py`**：遍历全部边**仅改写 relation 字段**，映射不到的归入「待审核」哨兵桶（原名存 `relation_raw`，不丢边、不重建结构）；同键撞边按 GraphStore 语义合并（evidence 并集、confidence 取高）
- 落盘前自动备份：`plot_graph_wu_ming_zhe.json.bak_relnorm_20260909_173330`

### 第二步：从源头约束抽取
- **`graph_extract.py`**（lore 层）与 **`plot_graph.py`**（剧情层，中/英两段提示词）均显式枚举 77 个允许关系类型，附规则：必须从词表选最接近谓语、映射不到直接丢弃、禁止否定词作关系名、长度/标点/拉丁字符约束。两处共用同一词表，永不漂移
- 双落点强制过滤：`normalize_extraction()` 与 `build_plot_graph()` 均接入 `canonicalize_relation`——**缓存里旧抽取结果入库时也会被兜住**；`build_plot_graph` 新增 `rel_filtered` 统计项
- 新提示词服从度实测：重建过程中最近 40 块、102 条关系，**词表外 0 条**
- 回归：`tests/test_plot_graph.py` + `tests/test_rag_graphrag_audit.py` **52 全过**（仅把测试夹具自由谓语「同僚」改为受控词「同伴」）

### 第三步：全量重建 lore 图谱
- `scripts/build_graph.py --character wu_ming_zhe --rebuild`，清缓存后多轮构建（pass2 抽取 + pass3/4 补失败块 + pass4 修复证据 ns 重建 + pass5/6 失败块清零）
- **失败块补抽（云端策略）**：本地 qwen2.5:7b 对 49 个 ASR 噪声重的中英混排块稳定产出畸形 JSON（`"relations[]}` 缺冒号）。按「直接传到云端由云端模型处理」的决策，这 49 块由大模型直接逐块人工抽取（脚本 `scripts/backfill_failed_chunks_cloud.py` + `backfill_last_chunk.py`，结果以 GraphCache 格式写入缓存，入库仍过 `normalize_extraction` 词表归一化兜底），全程零 API key 依赖
- **最终产出：`data/knowledge/graph_wu_ming_zhe.json` —— 实体 3846、边 3608，2831/2831 块失败清零**，实体向量全量，schema=v1

### 顺带修掉的存量 bug（治理中发现的致命问题）
- **`scripts/build_graph.py _load_chunks`**：只传 `"ns"` 键而 `build_graph_from_chunks` 读 `"namespace"` 键 → 全部证据 ns 落到兜底 `"lore"` → lore 层证据回查恒空（`GraphSearcher.retrieve` 永远返回空）。修复后 lore 图谱检索才真正可用——此 bug 在旧图谱上同样存在，与本次治理无关但被烟雾测试暴露

---

## 二、量化验证对比

### 1. 关系词频分布（治理目标：唯一名 ≤300、单例占比 ≤40%）

| 指标 | plot 归一化前 | plot 归一化后 | lore 重建后（最终） |
| --- | --- | --- | --- |
| 边数 | 4261 | 4035 | 3608 |
| **唯一关系名** | **1564** | **78** ✅ | **75** ✅ |
| **单例谓语占唯一名比** | **72.1%** | **0%** ✅ | **0%** ✅ |
| 单例谓语占边比 | 26.5% | 0% | 0% |
| Top10 谓语覆盖边比 | 23.9% | 42.3% | 46.3% |

plot 层 78 = 77 canonical + 1「待审核」哨兵桶（270 条边，269 个原名待人工审，见附录报告）；lore 层 75 = 从源头只产出 canonical。
lore Top15：对抗(270)/位于(252)/隶属(251)/前往(197)/保护(143)…

### 2. 检索烟雾测试（16 条代表性查询，`lexical_fallback=0`，include_weak=True）

| 指标 | plot 归一化前 | plot 归一化后 | lore 重建后 |
| --- | --- | --- | --- |
| 非空率 | 16/16 (100%) | 16/16 (100%) | **13/16 (81.3%)** |
| 图谱路命中块 | 48 | 48 | 39（100% 走图谱路，零词法兜底） |
| 平均每查询命中 | 3.0 | 3.0 | 2.4 |

- plot 层：非空率本来就饱和（实体链接按名字命中），治理收益体现在**关系同义收敛**（Top10 集中度 23.9%→42.3%，PPR 边权不再被几十个同义谓语稀释）
- lore 层：修复证据 ns bug 前非空率 **0/16**，修复后 **13/16**——提升完全来自治理链路（词表抽取 + 证据可回查）
- 3 个空查询（阿黛尔/纳西索斯/玩具盒）的实体在 **lore 语料中根本不存在**（纯剧情层角色/物品），属语料覆盖边界，非检索缺陷

### 3. 构建过程数据
- 2831 块处理：**缓存命中 2831 / 失败 0（最终 pass）**，空抽取跳过 1286（ASR 噪声块无实体可抽，正常）
- 提示词约束生效：新抽关系 100% 落在词表内，无需事后修正
- 49 个本地模型失败块全部由云端策略补抽完成，覆盖率 100%

---

## 三、结论与 P4 A/B 决策建议

**两项治理目标全部达成**（唯一名 1564→78/75，单例占比 72.1%→0%，远优于 ≤300 / ≤40% 目标线），且检索链路从「lore 图谱路恒空」修复为「81% 非空 + 命中全走图谱路」，构建失败块清零（2831/2831）。**建议进入 P4 A/B 测试**：
- A 组：现配置（图谱路可用），B 组：`plot_graph_enabled=false / graph_enabled=false`
- 观测指标：回答的剧情事实准确率、关系脉络块被引用率、幻觉率；重点看「隶属/对抗/保护」类问题
- 风险提示：270 条待审核边（6.3%）是已知尾巴，不影响 A/B 启动；建议 A/B 期间同步人工审一遍「待审核」桶（报告附录有完整清单）
